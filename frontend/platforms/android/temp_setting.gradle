android {
            defaultConfig {
                applicationId = "org.nativescript.ngsample"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }