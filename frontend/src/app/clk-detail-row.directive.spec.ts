import { ClkDetailRowDirective } from './clk-detail-row.directive';

describe('ClkDetailRowDirective', () => {
  it('should create an instance', () => {
    const directive = new ClkDetailRowDirective();
    expect(directive).toBeTruthy();
  });
});
